# pnpm 11 hard-errors on dependency build scripts unless allow-listed.
# These install native binaries (lightningcss-cli's minifier; wrangler's
# esbuild/sharp/workerd).
allowBuilds:
  lightningcss-cli: true
  esbuild: true
  sharp: true
  workerd: true
