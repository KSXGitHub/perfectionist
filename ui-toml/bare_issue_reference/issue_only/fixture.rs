/// Bad: closes #42 — with only `suggest_issue_url` enabled, the
/// single inline suggestion is `MachineApplicable`:
/// `[#42](https://github.com/owner/repo/issues/42)`.
fn _doc_issue_only() {}

fn main() {}
